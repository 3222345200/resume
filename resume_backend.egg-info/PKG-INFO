Metadata-Version: 2.4
Name: resume-backend
Version: 0.1.0
Summary: Online LaTeX resume system backend
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: fastapi<1.0.0,>=0.115.0
Requires-Dist: uvicorn[standard]<1.0.0,>=0.30.0
Requires-Dist: pydantic-settings<3.0.0,>=2.4.0
Requires-Dist: sqlalchemy<3.0.0,>=2.0.36
Requires-Dist: psycopg[binary]<4.0.0,>=3.2.3
Requires-Dist: jinja2<4.0.0,>=3.1.4

# Resume Backend

这是一个基于 `FastAPI` 的后端骨架，用于后续开发“在线 LaTeX 简历系统”。

## 当前已完成

- Python 后端项目结构初始化
- 环境变量模板
- 健康检查接口
- 应用配置加载
- PostgreSQL 数据库接入
- SQLAlchemy 模型与自动建表

## 目录结构

```text
.
├── app/
│   ├── api/
│   │   └── routes/
│   │       └── health.py
│   ├── core/
│   │   ├── config.py
│   │   └── db.py
│   ├── db/
│   │   └── base.py
│   ├── models/
│   │   └── resume.py
│   └── main.py
├── .env.example
├── .gitignore
├── pyproject.toml
└── README.md
```

## 本地启动

先确保机器上已经安装 Python 3.11 或更高版本。

### Windows PowerShell

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -e .
Copy-Item .env.example .env
uvicorn app.main:app --reload
```

首次接 PostgreSQL 时，先创建数据库：

```powershell
$env:PGPASSWORD='ResumeTemp#2026!'
& "C:\Program Files\PostgreSQL\16\bin\createdb.exe" -U postgres -h 127.0.0.1 -p 5432 resume_app
```

服务启动后访问：

- `http://127.0.0.1:8000/`
- `http://127.0.0.1:8000/health`
- `http://127.0.0.1:8000/docs`

## 下一步建议

1. 接入 PostgreSQL 与 SQLAlchemy / SQLModel
2. 增加简历模板与简历数据模型
3. 增加 LaTeX 渲染任务队列
4. 接入 PDF 生成服务
